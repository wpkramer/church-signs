<?xml version="1.0" encoding="utf-8"?>
    <!-- Sure, I'll share. Super simple app is just one window now, no pages no view models 
    this is MainWindow.xaml
    the App.xaml.cs constructs it and calls an async function in MainWindow to initialize the data 
    so Signs are loaded before this window is activated -->
<Window
    x:Class="ChurchSigns.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ChurchSigns"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:ChurchSigns.UI.Controls"
    xmlns:models="using:ChurchSigns.UI.Models"
    mc:Ignorable="d"
    Title="ChurchSigns">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Column="0" Grid.Row="0" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Show Templates Here:" Margin="24,0,0,24"/>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Paste Label Content Here:"/>
        </Grid>
        

        <GridView x:Name="SignGridView" Grid.Column="1" Grid.Row="0" 
                  Margin="24"
                  ItemsSource="{x:Bind Signs}"
                   BorderBrush="Black" BorderThickness="1" VerticalAlignment="Top">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="3" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:SignData">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <controls:SvgSignControl
                            SvgTemplate="{x:Bind Template, Mode=OneTime}"
                            Data="{x:Bind Fields, Mode=OneTime}"
                            Height="130"
                            Width="190"
                            RenderHeight="130"
                            RenderWidth="190"/>
                        <TextBlock Grid.Row="1" Text="{x:Bind Title}"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>


    </Grid>        

</Window>
